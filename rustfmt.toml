# Formatting is intentionally close to rustfmt defaults so contributions stay
# diff-friendly. Only stable options are set here.
edition = "2024"
max_width = 100
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
reorder_imports = true
reorder_modules = true
